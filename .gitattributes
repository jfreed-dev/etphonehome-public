# git-crypt encryption patterns
# Files matching these patterns are encrypted in the repository

# Secrets directory
.secrets/** filter=git-crypt diff=git-crypt

# Project settings (contains SSH keys, server configs)
.project_settings/** filter=git-crypt diff=git-crypt

# MCP config (may contain API keys)
.mcp.json filter=git-crypt diff=git-crypt

# Ansible secrets
ansible/secrets/*.yml filter=git-crypt diff=git-crypt
ansible/credentials/** filter=git-crypt diff=git-crypt
# Note: *.yml.example files won't match *.yml pattern, so they stay unencrypted

# Vault passwords
.vault_pass filter=git-crypt diff=git-crypt
.vault_password filter=git-crypt diff=git-crypt

# Private keys
*.pem filter=git-crypt diff=git-crypt
*.key filter=git-crypt diff=git-crypt
# Note: *.key.example files won't match *.key pattern, so they stay unencrypted

# Kubeconfig and Talosconfig
kubeconfig filter=git-crypt diff=git-crypt
talosconfig filter=git-crypt diff=git-crypt
