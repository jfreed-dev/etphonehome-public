# =============================================================================
# PRIVATE REPO .gitignore
# =============================================================================
# This is the private repository version. Secrets and credentials are TRACKED
# here (encrypted via git-crypt) but excluded from the public repo via
# .gitignore.public and the sync workflow.
#
# Files matching patterns in .gitattributes are encrypted with git-crypt.
# See docs/private-repo-setup.md for setup instructions.
# =============================================================================

# Terraform
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ansible
*.retry
ansible/roles/geerlingguy.*/

# OS
.DS_Store
Thumbs.db
*~

# IDE (optional - remove if you want to share IDE configs)
.idea/
.vscode/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
.venv/
.venv-build/
venv/
*.egg-info/

# Build artifacts
build/phonehome/
dist/

# Pre-commit
.pre-commit-cache/

# Logs
*.log
logs/

# Firmware images (too large for git, use S3)
*.img
*.img.xz
*.raw
*.raw.xz

# =============================================================================
# TRACKED IN PRIVATE REPO (encrypted via git-crypt)
# =============================================================================
# The following are NOT ignored in the private repo. They are:
# - Tracked and encrypted via git-crypt (see .gitattributes)
# - Excluded from public sync (see .gitignore.public)
#
# Secrets and credentials:
#   .secrets/
#   .project_settings/
#   ansible/secrets/*.yml
#   ansible/credentials/
#   .vault_pass, .vault_password
#   *.pem, *.key
#   kubeconfig, talosconfig
#   .mcp.json
#
# AI coding assistant context:
#   CLAUDE.md, AGENT.md, AGENTS.md, GEMINI.md, CODEX.md
#   .claude/, .gemini/, .codex/
# =============================================================================

# Temporary working files
tmp_*
